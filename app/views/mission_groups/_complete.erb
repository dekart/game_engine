<%= builder.title t(".title") %>

<%= builder.notice t(".message") %>

<% if result.group_payouts.try(:any?) %>
  <div class="payouts">
    <div class="received">
      <h3><%= t(".you_received") %></h3>

      <%= payout_list(result.group_payouts, :add) %>
    </td>
  </div>
<% end %>
