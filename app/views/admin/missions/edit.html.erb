<%= admin_title(t(".title", :name => @mission.name)) %>

<%= render("controls") %>

<% form_for([:admin, @mission], :html => {:multipart => true}) do |form| %>
  <%= form.fields %>

  <% dom_ready do %>
	$('form.edit_mission').submit(function(){
		if ($('#requirement_list').children().size() == 0) {
	      $('<input type="hidden" />').
		    attr({
		      name: "mission[requirements]",
		      value: ""
		    }).
		    appendTo($('#requirement_list'));
		}
		if ($('#payout_list').children().size() == 0) {
	      $('<input type="hidden" />').
		    attr({
		      name: "mission[payouts]",
		      value: ""
		    }).
		    appendTo($('#payout_list'));
		}
		return true;
	});
  <% end %>

  <%= form.submit_and_continue %>
<% end %>
