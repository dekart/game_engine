<%= payout(:attack, item.attack > 0 ? "+%d" % item.attack : item.attack) if item.attack != 0 %>
<%= payout(:defence, item.defence > 0 ? "+%d" % item.defence : item.defence) if item.defence != 0 %>

<%= payout_list(item.payouts, :add, :preview) %>
