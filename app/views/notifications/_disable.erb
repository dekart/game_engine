<div class="disable_notification">
  <%= link_to_remote(t(".disable"),
        :url      => disable_notification_path(notification),
        :confirm  => t(".confirm", :type => notification.title),
        :before   => "$.dialog.close(); #{ga_track_event('Notifications', 'Disable', notification.title)}"
      ) %>
</div>
