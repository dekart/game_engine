<%= title(t(".title")) %>

<% form_for [:admin, @group], :url => admin_mission_groups_url(:canvas => false), :html => {:multipart => true} do |form| %>
  <%= render :partial => "form_fields", :locals => {:form => form} %>

  <%= submit_tag t(".submit") %>
<% end %>