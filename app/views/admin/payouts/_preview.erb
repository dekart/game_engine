<div class="<%= payout.name %>">
  <% if payout.respond_to?(:state) and payout.state != 'visible' %>
    <span class="state"><%= admin_state(payout, :controls => false) %></span>
  <% end %>

  <%= payout %>
</div>
