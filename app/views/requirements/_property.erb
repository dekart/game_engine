<% requirement(requirement.name, satisfied) do %>
  <p>
    <span class="image">
      <%= property_image(requirement.property_type, :icon) %>
    </span>

    <span class="label">
      <% if requirement.display_level? %>
        <%= t(".text",
              :name   => content_tag(:span, requirement.property_type.name, :class => :value),
              :level  => requirement.level
            ).html_safe! %>
      <% else %>
        <span class="value"><%= requirement.property_type.name %></span>
      <% end %>
    </span>
  </p>
<% end %>
