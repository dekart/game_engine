<% if @success %>
  <%= fb_i("Well done!") %>
  <%= fb_i(@mission.won_text) %>

  <script>
    Character.update(<%= current_character.to_json %>);
  </script>
<% else %>
  <%= fb_i("You don't have enough energy to fulfill this mission. Take a rest.") %>
<% end %>