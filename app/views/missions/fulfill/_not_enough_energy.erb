<div class="buttons">
  <%= link_to(button(:refill), premium_path,
        :class => "refill_energy button"
      ) %>
</div>

<p>
  <%= raw t(".message",
        :ep_number      => content_tag(:strong, mission.ep_cost),
        :minute_number  => content_tag(:strong, (current_character.ep_restore_time(mission.ep_cost) / 1.minute).ceil)
      ) %>
</p>

<p>
  <%= raw t(".get_more",
        :premium_page => link_to(t(".premium_page"), premium_path)
      ) %>
</p>
