<%= link_to_remote(h(@translation.value),
      :url    => edit_admin_translation_path(@translation),
      :method => :get,
      :update => translation_id(@translation.key)
    ) %>

<% dom_ready do %>
  $('#<%= translation_id(@translation.key) %>').parent().addClass('customized');
<% end %>