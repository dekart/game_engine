<div id="property_result">
  <% if @property %>
    <p>
      <%= raw t(".messages.success",
            :amount         => @amount,
            :property_name  => content_tag(:b, @amount > 1 ? @property.plural_name : @property.name)
          ) %>
    </p>

    <h3><%= t(".you_received.title") %></h3>

    <div class="payouts">
      <%= payout(:basic_money, number_to_currency(@property.basic_money)) %>
    </div>
  <% else %>
    <%= t(".messages.property_not_found") %>
  <% end %>
</div>

<% fb_js_string :list do %>
  <%= render :partial => "list", :locals => {:properties => @properties} %>
<% end %>

<% content_for :script do %>
  $('property_list').setInnerFBML(list);
  $('property_income_value').setTextValue('<%= number_to_currency(current_character.property_income) %>')
  $('result').show();
<% end %>