<% if @newsfeed.any? %>
  <h2 class="title">
    <%= @character == current_character ?
            t(".title_my") :
            t(".title", :user => character_name(@character, :possessive => true)) %>
  </h2>

  <% @newsfeed.each do |news| %>
    <div class="news">
      <%= render :partial => "newsfeeds/#{news.class.to_s.underscore}", :locals => {:news => news} %>
    </div>
  <% end %>
<% end %>
