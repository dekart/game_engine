<%= title(t(".title")) %>

<% form_for [@victim, @hit_listing] do |form| %>
  <%= form.field :victim, character_name_link(@victim) %>

  <%= form.text_field :reward %>

  <%= form.submit %>
<% end %>

<div class="note"><%= t(".note", :fee => Setting.i(:hit_list_reward_fee)) %></div>