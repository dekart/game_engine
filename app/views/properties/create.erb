<div id="property_result">
  <% if !@property.new_record? %>
    <%= link_to(fb_i(t(".buttons.properties")), properties_url(:canvas => true), :class => "property button") %>

    <p>
    <% fb_i do %>
      <%= t(".messages.success") %>
      
      <%= fb_it(:property_name, content_tag(:b, fb_i(@property.name))) %>
    <% end %>
    </p>

    <h3><%= fb_i(t(".you_spent.title")) %></h3>
    <ul>
      <% if @property.basic_price.to_i > 0 %>
        <li class="basic_money">
          <b><%= fb_i(Character.human_attribute_name("basic_money")) %></b>
          <div><%= @property.basic_price %></div>
        </li>
      <% end %>

      <% if @property.vip_price.to_i > 0 %>
        <li class="vip_money">
          <b><%= fb_i(Character.human_attribute_name("vip_money")) %></b>
          <div><%= @property.vip_price %></div>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p>
      <% fb_i do %>
        <%= @property.errors.on(:character) %>
        <%= fb_it(:property_name, content_tag(:b, fb_i(@property.name))) %>
      <% end %>
    </p>
  <% end %>
</div>

<% content_for :script do %>
  $('result').show();
<% end %>