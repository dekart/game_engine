<%= admin_title(t(".title", :name => @contest.name)) %>

<%= form_for([:admin, @contest], :html => {:multipart => true}) do |form| %>
  <%= form.fields %>

  <%= form.submit_and_continue %>
<% end %>
