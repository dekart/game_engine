<div class="boost <%= "#{destination} #{boost.boost_type} #{'active' if boost.active_boost?(destination)}" %>" 
  id="<%= boost_dom_id(boost, destination) %>">
  <%= link_to(inventory_item_image(boost, :small, :tooltip => true, :count => true),
        toggle_boost_inventory_path(boost, :destination => destination),
        :remote => true,
        :method => :post
      ) %>
</div>