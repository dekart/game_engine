<% Item::EFFECTS.each do |effect| %>
  <% if value = item.send(effect) and value != 0 %>
    <%= payout(effect, value > 0 ? "+%d" % value : value) %>
  <% end %>
<% end %>

<%= payout_list(item.payouts, :add, :preview) %>
