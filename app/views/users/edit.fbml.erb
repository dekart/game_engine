<%= title(fb_i(t(".title"))) %>

<% form_for(@user) do |form| %>
  <%= form.check_box(fb_i(User.human_attribute_name("show_next_steps")), :show_next_steps) %>

  <% fb_tag(:input) do %>
    <%= fb_ta(:type, :submit) %>
    <%= fb_ta(:value, fb_i(t(".save"))) %>
  <% end %>
<% end %>