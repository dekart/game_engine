<%= title(fb_i("Your Alliance")) %>

<div class="note">
  <p>
    <% fb_i do %>
      Your alliance gives you additional attack and defence points. The more
      members in your alliance, the more chances you have in every battle.
    <% end %>
  </p>

  <%= link_to("Recruit to Alliance", invite_users_path, :id => :alliance_recruit) %>
</div>

<% if @relations.any? %>
  <table id="alliance_members" cellspacing="0">
    <% @relations.each do |relation| %>
      <tr>
        <td class="picture"><%= link_to(fb_profile_pic(relation.target.user, :linked => false), relation.target) %></td>
        <td class="name"><%= link_to(fb_name(relation.target.user, :linked => false), relation.target) %></td>
        <td class="level"><%= fb_i("Level:") %> <%= relation.target.level %></td>
      </tr>
    <% end %>
  </table>

  <%= will_paginate(@relations) %>
<% else %>
  <div class="empty_set">
    <%= fb_i("You don't have any members in your Alliance") %>
  </div>
<% end %>