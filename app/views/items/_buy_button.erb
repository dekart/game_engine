<%= form_tag(inventories_path, :remote => true, :class => :clearfix) do %>
  <%= amount_select_tag(:id => item.id, :vip_price => item.vip_price, :basic_price => item.basic_price) %>
  <%= hidden_field_tag(:item_id, item.id) %>

  <%= link_to_function(button(:buy), "Shop.confirmPurchase(this)",
        :class => "buy button"
      ) %>
<% end %>