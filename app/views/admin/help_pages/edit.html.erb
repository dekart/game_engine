<%= admin_title(t(".title", :name => @page.name)) %>

<% form_for [:admin, @page] do |form| %>
  <%= render :partial => "form_fields", :locals => {:form => form} %>

  <%= submit_tag t(".submit") %>
<% end %>