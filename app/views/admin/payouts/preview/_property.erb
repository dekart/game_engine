<% content_tag :div, :class => payout.name do %>
  <%= "%s (%s %s %d%%)" % [
        payout.property_type.name,
        payout.apply_on,
        payout.action,
        payout.chance
      ] %>
<% end %>