<%= payout(:item, boost_image(payout.boost, :icon),
      :label => t("payouts.boost.label",
        :name   => content_tag(:span, payout.boost.name, :class => :name),
        :amount => payout.amount
      ).html_safe
    ) %>
