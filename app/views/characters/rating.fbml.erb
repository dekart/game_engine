<%= title(fb_i(t(".title"))) %>

<table id="rating">
  <tr>
    <th>#</th>
    <th colspan="3"><%= fb_i(t(".columns.user")) %></th>
    <th><%= fb_i(t(".columns.rating")) %></th>
  </tr>
  <% @characters.each_with_index do |character, index| %>
    <tr>
      <td class="position"><%= index + 1 %></td>
      <td class="picture"><%= character_picture_link(character) %></td>
      <td class="name"><%= character_name_link(character) %></td>
      <td class="level"><%= Character.human_attribute_name("level") %> <%= character.level %></td>
      <td class="rating"><%= character.rating %></td>
    </tr>
  <% end %>
</table>