<%= admin_title(t(".title", :name => @asset.alias)) %>

<% form_for [:admin, @asset], :html => {:multipart => true} do |form| %>
  <%= render :partial => "form_fields", :locals => {:form => form} %>

  <%= submit_tag t(".submit") %>
<% end %>