<%= admin_title(t(".title")) %>

<%= render "controls" %>

<table>
  <tr>
    <th><%= t(".parameter") %></th>
    <th><%= t(".value") %></th>
    <th><%= t(".change") %></th>
    <th></th>
  </tr>
  <tr>
    <td><%= t(".total_users") %></td>
    <td><%= @total_users %></td>
    <td>+ <%= @last_day_users %></td>
    <td><%= link_to(t(".details"), user_admin_statistics_path) %></td>
  </tr>
</table>
