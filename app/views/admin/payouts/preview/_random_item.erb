<% content_tag :div, :class => payout.name do %>
  <%= "Random Item %s %d %s (%s %s %d%%)" % [
        payout.availability || "any",
        payout.value,
        payout.allow_vip ? "vip" : "basic",
        payout.apply_on,
        payout.action,
        payout.chance
      ] %>
<% end %>